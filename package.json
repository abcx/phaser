{
  "name": "phaser",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run clean && npm run build",
    "build": "node scripts/start.js",
    "clean": "del-cli --force ./build ./.cache",
    "build2": "PORT=process.env.PORT node scripts/start.js",
    "dev": "parcel watch *.js, *.scss, *.json"
  },
  "engines": {
    "node": "14.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "phaser": "^3.55.2"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "marked": "^4.0.12",
    "parcel-bundler": "1.12.4",
    "sass": "1.32.5"
  }
}
